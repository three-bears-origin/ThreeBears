<h2>Contribution: <%= @event.title %></h2>
<%= form_with(model: [ @event, @event.contributions.build ], local: false) do |form| %>
  <p>
    <%= form.text_area :text, size: "60x12" %>
  </p>
  <p>
    <%= form.submit %>
  </p>
<% end %>