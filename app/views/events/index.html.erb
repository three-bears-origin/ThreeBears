<body>
  <header>
    <!--add link later-->
    <a href="" class="logo">
      <img src="" alt="Logo">
    </a>
    <div class="search-bar">
      <!--get search bar working later-->
      <h3>Search</h3>
      <%= form_with local: true do |form| %>
        <%= form.text_field :text %>
      <% end %>
    </div>
    <a href="" class="help">
      <img src="" alt="Help">
    </a>
  </header>

  <div class="page">

    <aside class="nav-bar" id="nav-bar">
      <ul class="nav-icons">
        <li class="nav-item">
          <!--Change to link later-->
          <img src="">
          <h3>Profile</h3>
        </li>
        <li class="nav-item">
          <!--add link later-->
          <img src="">
          <h3>Timeline</h3>
        </li>
        <li class="nav-item">
          <!--Change to link later-->
          <img src="">
          <h3>Chat</h3>
        </li>
      </ul>
    </aside>

    <div class="timeline-container"> 
      <h1> Your Timeline </h1>

      <ol class="timeline" id="timeline">
        <% @events.each do |event| %>
          <div class = "clear"></div>
          <li class="timeline-event" id="timeline-event<%= event.id %>">
            <div class="date" id="date<%= event.id %>"><%= event.date %></div>
            <div class="box">
<<<<<<< HEAD
              <p class="title"><%= event.title %></p>
              <%= link_to(image_tag("trash-icon.svg", :class => "icon trash"), event_path(event),
              method: :delete, data: { confirm: 'Are you sure?' }) %>
=======
              <div class="title"><%= event.title %></div>
              <%= link_to image_tag("trash-icon.svg", :class => "icon trash"), event_path(event),
              method: :delete, data: { confirm: 'Are you sure?' } %>
>>>>>>> 05a7cdd91c61a0a979dc0f0a4f6bb5669a0c239f
              <%= image_tag("image-icon.svg", :class => "icon image" ) %>
              <%= image_tag("text-icon.svg", :class => "icon text" ) %>
              <%= image_tag("audio-icon.svg", :class => "icon audio" ) %>
              <%= link_to image_tag("new-contribution-icon.svg", :class => "icon new-contribution" ), "",
              class: 'new-contribution', data: {'js-new-contribution' => true} %>
            </div>
            <div class="collapsible collapsed"  id="collapsible<%= event.id %>">
              <%=event.contributions.each do |contribution|%>
                <p>
                  <strong>Contribution <%= contribution.id %>: </strong>
                  <%= contribution.text%>
                </p>
              <% end %>
            </div>
          </li>
        <% end %>
      </ol>

      <a class="new-event-button" href="<%=new_event_path%>">Create a new event</a>
      
    </div>

  </div>

</body>
    <%= javascript_pack_tag 'events_index' %>
